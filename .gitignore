# Локальный журнал статусов (не коммитить; см. StatusProject/templates/STATUS-LOG.template.md)
STATUS-LOG.md

# Секреты и локальные env в корне репозитория (шаблоны — см. исключение ниже)
.env
.env.local
.env.*.local
!.env.example

# Логи и временные файлы
*.log
npm-debug.log*

# ОС
.DS_Store
Thumbs.db
Desktop.ini

# IDE (не участвуют в сборке сайта)
.idea/

# Временные файлы Office
~$*

# Презентации и документы в корне репозитория (не Next.js)
/*.pptx
/*.docx

# Архив логотипов — в репозитории достаточно распакованных файлов в logo/
/logo.zip

# Экспорты Stitch: статичные HTML и скриншоты не входят в приложение;
# скрипт выгрузки при необходимости остаётся отслеживаемым.
/design/stitch-exports/*
!/design/stitch-exports/download_screens.ps1
